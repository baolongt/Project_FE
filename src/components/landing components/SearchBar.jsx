import React from "react";

const SearchBar = () => {
  return (
    <React.Fragment>
      <input className="searchBar" type="text" placeholder="Find game" />
    </React.Fragment>
  );
};

export default SearchBar;
